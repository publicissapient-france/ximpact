{
    feedback(id: "{{id}}", customerId:"{{customerId}}", xebianId:"{{xebianId}}", impactId:"{{impactId}}") {
        id,
        comment
        createdAt
        xebianId
        customerId
        impactId
    }
}
