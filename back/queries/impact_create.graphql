mutation {
    impact_create(description: "Etre rapide", xebianId: "{{xebianId}}", customerId: "{{customerId}}") {
        id
        description
        xebian_id
        customer {
            id
            email
            created_at
            updated_at
            firstname
            lastname
            company
        }
    }
}
