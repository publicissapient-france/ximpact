mutation {
    impact(description: "Etre rapide", xebianId: "{{xebianId}}", customerId: "{{customerId}}") {
        id
        description
        xebianId
        customer {
            id
            email
            createdAt
            updatedAt
            firstName
            lastName
            company
        }
    }
}
