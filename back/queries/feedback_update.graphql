mutation {
    feedback_update(id: "{{feedbackId}}", comment: "OKAY", badges: "[{id: 'people',label: 'PeopleFirst',description: 'son bien-être', value: false}]") {
        id
        comment
        badges
        created_at
        updated_at
    }
}
