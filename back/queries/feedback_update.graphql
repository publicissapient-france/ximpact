mutation {
    feedback_update(id: "{{feedbackId}}", comment:"OKAY", customerId:"{{customerId}}", xebianId:"{{xebianId}}", impactId:"{{impactId}}") {
        id
        comment
        createdAt
        updatedAt
        xebianId
        customerId
        impactId
    }
}
